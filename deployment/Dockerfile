# Placeholder Dockerfile for the E-O MA1DCNN inference server.
# Base this on an NVIDIA L4T / JetPack image matching your Jetson Nano's
# JetPack version once confirmed (see deployment/README.md).
#
# FROM nvcr.io/nvidia/l4t-base:<tag>
# WORKDIR /app
# COPY src/edge_fault_dx /app/edge_fault_dx
# COPY models_out/eo_ma1dcnn.engine /app/models_out/eo_ma1dcnn.engine
# RUN pip3 install -r requirements.txt
# CMD ["python3", "-m", "edge_fault_dx.deployment.inference_server"]
