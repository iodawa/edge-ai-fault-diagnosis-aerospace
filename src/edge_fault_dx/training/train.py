"""Training loop for the baseline MA1DCNN (Phase 3).

TODO: standard supervised training loop against fault-class labels;
reads hyperparameters from configs/baseline.yaml (or the winning Optuna
trial from optuna_search.py).
"""
